.container
  table.table.table-bordered
    thead
      tr
        td Competition
    tbody
      - @competitions.each do |competition|
        tr
          td
            = link_to competition.name, competition_path(competition.original_competition_id)
